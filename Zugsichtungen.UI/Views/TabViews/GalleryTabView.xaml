<customControls:TabView x:Class="Zugsichtungen.UI.Views.TabViews.GalleryTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Zugsichtungen.UI.Views.TabViews"
             xmlns:customControls="clr-namespace:Zugsichtungen.UI.Controls.CustomControls;assembly=Zugsichtungen.UI.Controls"
                        xmlns:converter="clr-namespace:Zugsichtungen.UI.Converter"
             mc:Ignorable="d" 
                       xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             d:DesignHeight="800" d:DesignWidth="1600">
    <customControls:TabView.Resources>
        <converter:ImageConverter x:Key="ImageConverter"
                                  PixelWidth="600" />
        <converter:DateOnlyToGermanDateConverter x:Key="DateOnlyToGermanDateConverter"/>

        <DataTemplate x:Key="GalleryItemTemplate">
            <materialDesign:Card Margin="2">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="300"/>
                        <ColumnDefinition Width="400" />
                    </Grid.ColumnDefinitions>

                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="auto"/>
                    </Grid.RowDefinitions>
                    
                    <ProgressBar Style="{StaticResource MaterialDesignCircularProgressBar}"
                                 Grid.RowSpan="4"
                                 Grid.Column="0"
                                 Width="100"
                                 Height="100" 
                                 IsIndeterminate="True"
                                 Visibility="{Binding IsThumbnailLoading, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                    <materialDesign:PackIcon Kind="ImageOff" 
                                             Grid.RowSpan="4"
                                             VerticalAlignment="Center"
                                             HorizontalAlignment="Center"
                                             Height="75"
                                             Width="75"                                             
                                             Visibility="{Binding IsNoThumbnailAvailable, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                    
                    <Image Grid.Column="0"
                           Grid.RowSpan="4"
                           Width="300"
                           Height="200"
                           Source="{Binding Thumbnail, Mode=OneWay}"
                           />

                    <Button Grid.Column="0"
                            Grid.RowSpan="4"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Top"
                            Content="{materialDesign:PackIcon Kind=ArrowExpand}"
                            Style="{StaticResource MaterialDesignIconButton}"
                            Command="{Binding ShowPictureCommand, Mode=OneTime}"/>

                    <TextBlock Text="{Binding Date, Mode=OneTime, Converter={StaticResource DateOnlyToGermanDateConverter}}" Grid.Column="1" 
                               FontSize="30"/>
                    <TextBlock Text="{Binding VehicleDesignation, Mode=OneTime}" Grid.Column="1" Grid.Row="1"
                               FontSize="30"/>
                    <TextBlock Text="{Binding Location, Mode=OneTime}" Grid.Column="1" Grid.Row="2"
                               FontSize="30"/>
                </Grid>
            </materialDesign:Card>
        </DataTemplate>
    </customControls:TabView.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <ListBox Grid.Column="0" ItemTemplate="{StaticResource GalleryItemTemplate}"
                 ItemsSource="{Binding GalleryList, Mode=OneTime}"
                 materialDesign:ListBoxItemAssist.ShowSelection="False"
                 materialDesign:ListBoxAssist.CanUserToggleSelectedItem="False"
                 materialDesign:ListBoxItemAssist.Cursor="Arrow">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel IsItemsHost="True"
                               HorizontalAlignment="Center"/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
        </ListBox>
    </Grid>
</customControls:TabView>
